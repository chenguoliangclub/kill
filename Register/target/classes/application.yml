server:
  port: 9000
spring:
  application:
    name: server
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:9000/eureka
    fetch-registry: false
    register-with-eureka: false